services:
  app:
    build: .
    image: fitness-app:prod
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://leom.johnson:password@host.docker.internal:5432/fitness
      - PORT=8000
      - ENV=prod
    restart: unless-stopped   # auto-restart if container crashes
